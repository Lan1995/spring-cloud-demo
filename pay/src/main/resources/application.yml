spring:
  application:
    name: pay-application
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: dev
        cluster-name: pay
        group: one
        service: ${spring.application.name}
server:
  port: 19999