server:
  port: ${random.int(10000,19999)}

spring:
  application:
    name: order
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      discovery:
        namespace: dev
        cluster-name: order